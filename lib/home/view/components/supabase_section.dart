import 'package:flutter/material.dart';

class SupabaseSection extends StatelessWidget {
  const SupabaseSection({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Coming Soon...', style: Theme.of(context).textTheme.titleLarge));
  }
}
